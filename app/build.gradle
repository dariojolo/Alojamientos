apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'dariojolo'
            keyPassword 'casla2586'
            storeFile file('C:/Users/IBM_ADMIN/Documents/Dario/Android/firma.jks')
            storePassword 'casla2586'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "22.0.1"
    defaultConfig {
        applicationId "com.dariojolo.alojamientos"
        minSdkVersion 16
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.opencsv:opencsv:3.5'
    compile 'com.android.support:appcompat-v7:23.+'
    compile 'com.google.android.gms:play-services:+'
}
